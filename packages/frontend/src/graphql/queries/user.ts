/**
 * GraphQL queries for User.
 *
 * ⚠️ AUTO-GENERATED BY PRISM - DO NOT EDIT
 */

import { graphql } from '../gql';

/** Get a single User by ID */
export const GetUserQuery = graphql(`
  query GetUser($id: Int!) {
    user(id: $id) {
      id
      email
      passwordHash
      emailVerified
      emailVerificationToken
      mfaEnabled
      mfaSecret
      subdomainLimit
      isAdmin
      createdAt
      updatedAt
      deletedAt
      apiKeys {
        id
      }
      subdomains {
        id
      }
    }
  }
`);

/** List users with pagination */
export const ListUsersQuery = graphql(`
  query ListUsers($pagination: OffsetPaginationInput) {
    users(pagination: $pagination) {
      edges {
        node {
          id
          email
          passwordHash
          emailVerified
          emailVerificationToken
          mfaEnabled
          mfaSecret
          subdomainLimit
          isAdmin
          createdAt
          updatedAt
          deletedAt
          apiKeys {
            id
          }
          subdomains {
            id
          }
        }
        cursor
      }
      pageInfo {
        hasNextPage
        hasPreviousPage
        startCursor
        endCursor
      }
      totalCount
    }
  }
`);
