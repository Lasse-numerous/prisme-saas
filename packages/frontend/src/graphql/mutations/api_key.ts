/**
 * GraphQL mutations for APIKey.
 *
 * ⚠️ AUTO-GENERATED BY PRISM - DO NOT EDIT
 */

import { graphql } from '../gql';

/** Create a new APIKey */
export const CreateAPIKeyMutation = graphql(`
  mutation CreateAPIKey($input: APIKeyInput!) {
    createAPIKey(input: $input) {
      id
      userId
      keyHash
      keyPrefix
      name
      lastUsedAt
      expiresAt
      isActive
      createdAt
      updatedAt
      user {
        id
      }
    }
  }
`);

/** Update an existing APIKey */
export const UpdateAPIKeyMutation = graphql(`
  mutation UpdateAPIKey($id: Int!, $input: APIKeyUpdateInput!) {
    updateAPIKey(id: $id, input: $input) {
      id
      userId
      keyHash
      keyPrefix
      name
      lastUsedAt
      expiresAt
      isActive
      createdAt
      updatedAt
      user {
        id
      }
    }
  }
`);

/** Delete a APIKey */
export const DeleteAPIKeyMutation = graphql(`
  mutation DeleteAPIKey($id: Int!) {
    deleteAPIKey(id: $id)
  }
`);
