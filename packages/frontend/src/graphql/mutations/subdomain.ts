/**
 * GraphQL mutations for Subdomain.
 *
 * ⚠️ AUTO-GENERATED BY PRISM - DO NOT EDIT
 */

import { graphql } from '../gql';

/** Create a new Subdomain */
export const CreateSubdomainMutation = graphql(`
  mutation CreateSubdomain($input: SubdomainInput!) {
    createSubdomain(input: $input) {
      id
      name
      ownerId
      ipAddress
      status
      dnsRecordId
      port
      releasedAt
      cooldownUntil
      createdAt
      updatedAt
      owner {
        id
      }
    }
  }
`);

/** Update an existing Subdomain */
export const UpdateSubdomainMutation = graphql(`
  mutation UpdateSubdomain($id: Int!, $input: SubdomainUpdateInput!) {
    updateSubdomain(id: $id, input: $input) {
      id
      name
      ownerId
      ipAddress
      status
      dnsRecordId
      port
      releasedAt
      cooldownUntil
      createdAt
      updatedAt
      owner {
        id
      }
    }
  }
`);

/** Delete a Subdomain */
export const DeleteSubdomainMutation = graphql(`
  mutation DeleteSubdomain($id: Int!) {
    deleteSubdomain(id: $id)
  }
`);
